pipeline {
    agent any
    stages {
        stage('Check CMD') {
            steps {
                bat 'where cmd'
                bat 'java -version'
                bat 'mvn -version'
            }
        }
    }
}
