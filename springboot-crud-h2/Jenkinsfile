pipeline {
    agent any
    stages {
        stage('Check PATH') {
            steps {
                bat 'echo %PATH%'
                bat 'where cmd'
                bat 'java -version'
                bat 'mvn -version'
            }
        }
    }
}
